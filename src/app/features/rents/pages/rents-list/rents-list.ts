import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-rents-list',
  imports: [],
  template: `RentsList works!`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export default class RentsList {}